/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  allowedDevOrigins: ['192.168.100.97','local-origin.dev', '*.local-origin.dev'],
};

export default nextConfig;
